# Assets & facts to do

Every missing asset and unverified fact goes here as it's discovered. Nothing
gets fabricated to fill a gap — a row here and a `Placeholder` component instead.

| Pfad | Was fehlt | Format / Maße | Status |
| ---- | --------- | ------------- | ------ |
| `src/lib/siteUrl.ts` | Bestätigte Produktions-Domain in Vercel gesetzt | — | `.env.example`/`docs/deployment.md` (seit 2026-08-16) tragen `https://www.enactus-mannheim.com` als lokalen Default ein — die Domain der alten Webflow-Seite, übernommen als Arbeitsannahme, nicht als vom Vorstand bestätigte neue Domain. In Vercels Projekteinstellungen ist `NEXT_PUBLIC_SITE_URL` noch nicht gesetzt; `sitemap.ts`/`robots.ts` fallen bis dahin auf Vercels automatische Deployment-URL zurück statt eine Domain zu raten (`src/lib/siteUrl.ts`, unverändert) — sobald der Vorstand die Domain bestätigt, in den Vercel-Projekteinstellungen setzen |
| `src/lib/productionDeployment.ts` | Site tatsächlich unter `enactus-mannheim.com`/`www.enactus-mannheim.com` in Vercel bereitgestellt | — | Das Indexierungs-Gate selbst ist seit 2026-08-16 korrekt (prüft `VERCEL_ENV`, serverseitig, ohne die vorher nötige Vercel-Projekteinstellung — siehe `docs/deployment.md`). Der verbleibende Rest ist rein organisatorisch: `PRODUCTION_HOSTS` ist bewusst eine eigene, hartcodierte Liste (nicht aus `NEXT_PUBLIC_SITE_URL` abgeleitet) — solange die Domain in Vercel nicht diesem Projekt zugewiesen ist, bleibt jede Produktions-Auslieferung noindexed, unabhängig vom `VERCEL_ENV`-Wert |
| `public/brand/enactus-mannheim-logo-full.png`, `-full-on-dark.png`, `-mark.png` | Logo als SVG (alle drei Varianten sind aktuell PNG) | SVG, transparent | PNGs von der alten Website (enactus-mannheim.com) übernommen, 2026-07-29 — reicht fürs Web, ist aber Rasterware. Volle Variante: Original-Export in Schwarz (`-full.png`, für helle Flächen) und Weiß (`-full-on-dark.png`, für dunkle Flächen — war auf der alten Seite bereits als Export vorhanden, keine Umfärbung). Kompakte Variante (`-mark.png`, nur die goldene Bildmarke, quadratisch zugeschnitten): kein fertiger Ausschnitt existierte, selbst aus der vollen Variante freigestellt. Eine SVG-Quelldatei wäre für verlustfreie Skalierung vorzuziehen — beim Vorstand anfragen |
| `content/navigation.ts` (`networkLinks`) | Enactus Global-URL (`https://enactus.org`) | — | Gesetzt und laut Wikipedia-Eintrag sowie Websuche als offizielle globale Enactus-Website bestätigt. Ein direkter automatisierter Abruf war nicht möglich (HTTP 403, vermutlich Bot-Schutz) — vor Launch einmal manuell im Browser öffnen und bestätigen |
| `app/[locale]/(site)/page.tsx` | Startseite | — | Alle acht Sections stehen (Hero, Partner, Kennzahlen, Säulen, Benefits, Alumni, Vorstand, CTA); Copy dort, wo sie nicht aus `content/` kommt, ist Entwurf — siehe die Zeile zu `Benefits.*` und den Content-Dateien unten. Keine erfundene Mission/Vision-Copy über das Bestehende hinaus. Eigener Charakter durch drei Eingriffe ergänzt: der goldene Faden (`components/motion/ThreadSegment.tsx`, `threadRoute.ts`) von `PartnerMarquee` bis `ClosingCta` — seit 2026-08-16 ausschließlich hier, nicht mehr auf den Unterseiten, das editoriale Alumni-Layout (`AlumniVoices.tsx`), der Proximity-Effekt auf den Vorstandsportraits (`ProximityGroup.tsx`) |
| `app/[locale]/(site)/datenschutz` | Freigabe durch den Datenschutzbeauftragten von Enactus Germany | — | Vollständiger DE+EN-Text seit 2026-08-16, 18 Abschnitte (`Datenschutz.tsx`), inklusive Speicherdauer- und Auftragsverarbeiter-Tabelle (auf 360px als Definitionsliste). Keine `PlaceholderMark`-Elemente mehr auf der Seite — alle Fakten sind gesetzt. Seite zeigt weiterhin den Entwurfshinweis (`content/privacy.ts`'s `reviewed: false`) — kippt auf einen Satz um, sobald der Vorstand/DPO freigibt. Zwei offene Abweichungen zwischen Text und Code, dem Vorstand zur Entscheidung vorgelegt: (1) die Kontaktanfragen-Frist ist als „12 Monate nach abschließender Bearbeitung" formuliert, aber die Löschroutine (`lib/db.ts`'s `deleteExpiredContactMessages`) zieht ausschließlich `created_at` heran — es gibt keine „bearbeitet"-Markierung in `contact_messages`; (2) `APPLICATION_RECIPIENT_EMAIL` ist weder in `.env.local` noch `.env.example` gesetzt, obwohl der Text `info@unimannheim.enactus.team` als Empfänger nennt — muss vor Launch in Vercel gesetzt werden |
| `content/org.ts` | Allgemeine Kontakt-E-Mail (öffentliche Anlaufstelle, getrennt von der Vorstands-/Impressum-Adresse) | — | Vereinssitz, Vereinsregister-Nummer und Vorstands-E-Mail sind seit 2026-07-29 gesetzt (siehe Impressum-Zeile oben). Nur die allgemeine Kontakt-E-Mail bleibt `null` |
| `content/org.ts` (`foundingYear`) | Bestätigung Gründungsjahr 2003 | — | `verified: false`. Zeigt seit 2026-08-15 als „Seit {year}" dezent im Footer statt als KPI (`content/kpis.ts` hat keinen `foundedYear`-Eintrag mehr) |
| `content/kpis.ts` (`spinoffs`) | Vier weitere Ausgründungen/Übergaben namentlich benennen | — | Der Vorstand geht intern von 9 Gegründet-&-Übergeben-Fällen aus, aktuell sind nur 5 namentlich belegt (Blauherz, Differgy, Sanagua, Stadthonig, Sunte). Die KPI (`content/kpis.ts`) bleibt bei `5`, bis die restlichen vier benannt sind — nicht vorher erhöhen |
| `content/projects.ts` (`resoap`) | Nachname und E-Mail des ReSoap-Leads | — | Vorname „Heidi" ist bestätigt, aber `leadName`/`leadEmail` bleiben `null`, statt nur den Vornamen zu speichern (sähe wie eine unvollständige Person aus, sobald das je gerendert wird) |
| `content/projects.ts` (Archiv: `sun-n-soil`, `green-heat`, `reverze`, `afya`, `mushroom`, `greenscape`) | Bestätigter `status` | — | Auf Rückfrage als Default gesetzt: `cancelled`, bis der Vorstand den tatsächlichen Status pro Projekt nennt (vgl. das frühere Safesteps/Vela-Beispiel, wo der Default korrigiert wurde) |
| `content/projects.ts` (Archiv-Projekte, ReSoap) | `stage`, `externalUrl`, `linkedinUrl`, `logo`, `images`, `sdgs`, `leads` | Logo/Bilder als Datei, sonst Text/URL | Für die vier aktiven Projekte seit 2026-08-16 großteils real: SDG-Fokus bei allen vieren, SmileGreens Prozessstufe (`mvp`) und LinkedIn-Seite, Mealyos Website, Logos für SmileGreen/Mealyo/ImpactWithUs, zwei SmileGreen-Projektfotos. Offen bleiben: alle zehn Archiv-Projekte (durchgehend `null`/`[]`), ReSoaps Logo/Fotos/Leads, Projektfotos für Mealyo und ImpactWithUs, Prozessstufe für Mealyo/ReSoap/ImpactWithUs, und Beschreibungstext für ReSoap sowie alle Archiv-Projekte (`ANREISSER_FEHLT`/`BESCHREIBUNG_FEHLT` als `PlaceholderMark`) |
| `content/projects.ts` (`smilegreen`, zweite Projektleitung) | E-Mail und LinkedIn-Profil von Franka Zanolli | — | Name seit 2026-08-16 gesetzt, Kontaktdaten bleiben `null`. Schreibweise prüfen: der Handover nennt „Zanolli", das Namensschild auf `smilegreen-nationalcup-2.jpg` liest sich als „Zanoli" |
| `public/projects/smilegreen-logo.png` | Echte Vektorquelle des SmileGreen-Logos | SVG | Die gelieferte SVG-Datei war kein echtes Vektororiginal, sondern eine Autovektorisierung des JPEGs (3488 Pfade, 362 KB, deckender weißer Hintergrund). Stattdessen als transparentes PNG mit 512 px Höhe abgelegt — ausreichend für die 56-px-Karte und die Detailseite, für großformatige Verwendung aber ein echtes Vektororiginal beim Projektteam anfragen |
| `public/projects/mealyo-logo.png` | Echte Vektorquelle des Mealyo-App-Icons | SVG | Die gelieferte `MealyoAppIcon.svg` war ein 6-MB-SVG mit einem eingebetteten 1024×1024-PNG, also ebenfalls kein Vektor — als 512-px-PNG (89 KB) abgelegt |
| `content/projects.ts` (alle Einträge, `year`) | Start-/Ausgründungs-/Endjahr je Projekt | — | Feld seit 2026-08-15 als Teil des `/projekte/archiv`-Rasters (Logo, Jahr, Status) angelegt, für alle Projekte `null` — kein Jahr ist irgendwo dokumentiert, keins wird aus der Listenreihenfolge geraten |
| Projektfotos (`content/projects.ts`, `images`) und Foto der Projektleitung | Je aktivem Projekt bis zu drei Projektfotos, plus ein Foto je Projektleitung | Projektfotos 4∶3, Projektleitung Hochformat 3∶4 (wie beim Vorstand) | SmileGreen hat seit 2026-08-16 zwei echte Fotos (National Cup 2026), ImpactWithUs ein echtes Foto der Projektleitung (Finn Brämig). Alle übrigen Slots rendern weiterhin `Placeholder`, gleiches Muster wie `BoardGrid.tsx` |
| `messages/{de,en}.json` (`EventFormats.<key>.detail`) | Beschreibungstext je Event-Format (Socials, Workshops, Teamwochenende, Gala) | — | Platzhalter aktiv (`BESCHREIBUNG_FEHLT`/`DESCRIPTION_MISSING`), gerendert über `PlaceholderMark variant="missing"` auf `/events`. Die vier Titel selbst sind real (direkt vorgegeben), nur die längeren Beschreibungen fehlen. „Journeys" ist seit 2026-08-16 kein Format-Titel mehr — die Reisen-Historie hat mit `JourneysSection.tsx` bereits einen eigenen, ausführlicheren Abschnitt direkt darunter, „Gala" ersetzt den Slot |
| Bild fürs Format Teamwochenende (`content/eventFormats.ts`, `image`) | Ein Bild | Format offen | Platzhalter aktiv (`null`) — `/events` zeigt bis dahin `Placeholder` statt eines echten Bilds. Socials, Workshops und Gala haben seit 2026-08-16 echte Fotos aus dem Vorstands-Medien-Handover (`neue medien/`) |
| `messages/{de,en}.json` (`Process.steps.<milestone>.checklist`) | Prüfkriterien für die vier Gates (Kick-Off, Inno Gating, Operations Gating, Ausgründung) | — | Platzhalter aktiv (`PRÜFPUNKT_1`–`_3` / `CRITERION_1`–`_3`), gerendert über `PlaceholderMark variant="missing"` auf `/prozess`. Nie erfunden — mit dem Vorstand zu klären |
| `messages/{de,en}.json` (`Process.steps.<phase>.checklist`) | Vorteile der vier Phasen (Ideation, MVP, Projektumsetzung, Startup) | — | Entwurfstext, über `PlaceholderMark variant="unverified"` auf `/prozess` markiert — plausible, aber nicht vom Vorstand freigegebene Behauptungen; vor Launch gegenlesen lassen (gleiches Muster wie `Benefits.*`) |
| `content/process.ts` (`projectGuide`) | Project-Guide-PDF (kompletter Prozess zum Nachlesen) | PDF | Platzhalter aktiv (`available: false`, `href: null`) — Download-Button auf `/prozess` ist bis dahin nativ `disabled`, mit sichtbarem Hinweis (`ProjectGuideDownload.tsx`) |
| `content/stars.ts` | Logos für STAR_1–STAR_8; Fakten zu Stadthonig und Sunte bestätigen | SVG/PNG | Namen, Status und YouTube-IDs seit 2026-08-15 real (siehe Kommentar in `stars.ts`). Logos bleiben `null` für alle acht. Stadthonig und Sunte tragen `verified: false` — die Fakten selbst („an einen Partner übergeben" / „Solarstrom und Aufklärung an Krankenhäusern in Togo") sind noch nicht endgültig vom Vorstand bestätigt, gerendert über `PlaceholderMark variant="unverified"`, sobald `/projekte` das Stars-Segment baut |
| `content/alumni.ts` | Alumni-Einträge (Namen, aktuelle Position, Zitat, LinkedIn, Foto) | Foto: Hochformat 3∶4 | Platzhalter aktiv (missing) — 3 Einträge `ALUMNUS_1`–`ALUMNUS_3` mit `POSITION_n`/`STATEMENT_n`, Rest `null`. Format seit dem Editorial-Layout Hochformat statt quadratisch (Portrait bricht über die Container-Kante hinaus, siehe `AlumniVoices.tsx`) |
| `content/events.ts` | Veranstaltungskalender (Titel, Datum, Ort, URL) | — | Platzhalter aktiv (missing) — 4 Einträge, `title` als Message-Key, Rest `null` |
| `content/partners.ts` | Vektorisierte Logos für die Raster-Einträge | SVG bevorzugt | Namen, Tiers und Logos seit 2026-08-15 real, vom Webflow-CDN der alten Website in `public/brand/partners/` übernommen. Fünf liegen bereits als SVG vor (SZA, KPMG, SHUB Mannheim, MAFINEX, Heidelberg Materials); Freudenberg, Horbach, MCEI, ProCredit Bank, Fuchs Petrolub, Absolventum und E.ON Inhouse Consulting sind Raster (PNG/JPG) — beim jeweiligen Partner nach einer Vektor-Quelle fragen, falls verfügbar |
| `content/partners.ts` (`htgf`, `allianz-global-investors`, `basf`, `phoenix-group`, `pg`) | Partnerschafts-Tier und bestätigte Website je Partner | — | Fünf neue Logos für das Homepage-Logoband aus dem Vorstands-Handover (`neue medien/Logo Firmen/`) seit 2026-08-16 real, aber `tier`/`url` bleiben `null` — noch kein Tier zugewiesen, noch keine Website-URL nach demselben Muster wie die zwölf Partner oben abgerufen/bestätigt |
| `content/partners.ts` (`url`) | MCEI-Website bestätigen | — | Elf der zwölf Partner-URLs wurden am 2026-08-15 per Abruf bestätigt (siehe `/partner`-Build). `mcei.de` liefert aktuell HTTP 401 mit `WWW-Authenticate: Basic realm="Wartung"` — die Domain existiert, ist aber im Wartungsmodus gesperrt; nicht verlinkt, bleibt `null`, bis sie wieder erreichbar ist. `horbach.de` und die E.ON-Inhouse-Consulting-Unterseite (`eon.com`) blockieren automatisierte Abrufe mit HTTP 403 (Bot-Schutz), sind aber über mehrere unabhängige Suchtreffer als korrekt bestätigt und deshalb verlinkt — vor Launch trotzdem einmal manuell im Browser prüfen, gleiches Muster wie beim `enactus.org`-Eintrag oben. `mafinex.de` selbst ist als TLS-Ziel nicht erreichbar (Zertifikatsfehler); verlinkt ist stattdessen `mafinex.next-mannheim.de`, die bestätigt erreichbare Adresse der MAFINEX-Seite im NEXT-MANNHEIM-Netzwerk |
| `content/media.ts` (`heroMedia.mobileImageSrc`) | Mobiles Standbild fürs Hero (unter dem `md`-Breakpoint) | Format: gleiches Seitenverhältnis wie das Video | Video und Poster sind seit 2026-08-16 real (Bühnenpitch, Vorstands-Medien-Handover), der temporäre Verlaufshintergrund in `HomeHero.tsx` ist deshalb entfernt. Nur `mobileImageSrc` bleibt `null` — unterhalb `md` zeigt der Hero bis dahin nur die abgedunkelte Fläche ohne Bild |
| `content/mitmachenFit.ts` / `messages/{de,en}.json` (`MitmachenPage.fit.expectations.*`, `...offers.*`) | Freigabe der acht Erwartungs-/Angebots-Texte auf `/mitmachen` | — | Entwurfstext seit 2026-08-16 (vier Erwartungen inkl. „Agency", vier Angebote) — plausible, aber nicht mit dem Vorstand abgestimmte Behauptungen, gleiches Muster wie `content/benefits.ts`; vor Launch gegenlesen lassen |
| `content/mitmachenProcess.ts` / `messages/{de,en}.json` (`MitmachenPage.timeline.steps.*`) | Freigabe des dritten Prozessschritts „Rückmeldung" | — | „Bewerbung" ist durch `Faq.application-window` belegt, „Kennenlerngespräch" durch `Faq.choose-position`; „Rückmeldung" ist der selbstverständliche Abschluss jedes Bewerbungsprozesses, ohne konkrete Frist zu behaupten — trotzdem beim Vorstand gegenlesen lassen, bevor die drei Schritte als endgültig gelten |
| `content/recruiting.ts` | Zweiter Bewerbungszyklus (falls das Fenster 01.–13.09.2026 nicht die einzige Runde bleibt) | — | `/mitmachen` zeigt nach Ablauf von `closesAt` einen generischen „für diesen Zyklus geschlossen"-Hinweis ohne neues Datum zu erfinden — sobald ein zweiter Zyklus feststeht, `recruitingWindow` aktualisieren |
| `messages/en.json` (`Pillars.*`, `Benefits.*`, `AlumniVoices.*`, `BoardGrid.*`, `Board.*`, `ClosingCta.*`, `Process.*`, `Projects.smilegreen.*`, `Stars.*`, `ProjectsPage.*`, `ProjectsArchivePage.*`, `ProjectStatus.*`, `EventsPage.*`, `EventFormats.*`, `JourneysSection.*`, `EventsNetwork.*`, `PartnerPage.*`, `PartnerStatements.*`, `KontaktPage.*`, `MitmachenPage.*`) | Echte Übersetzung statt Erstentwurf | — | Bereits als eigenständiges Englisch formuliert (keine reine Spiegelung des Deutschen), aber Teil der noch ausstehenden dedizierten Übersetzungsrunde vor Launch |
| `src/lib/applicationPdf.tsx` | Selbst gehostete Schriftdatei (Geist) für das Bewerbungs-PDF | `.ttf`/`.otf`, für `Font.register` | Nutzt aktuell `@react-pdf/renderer`s eingebautes Helvetica. Die Website selbst bindet Geist/Instrument Serif über `next/font/google` ein, das zur Buildzeit selbst hostet und keine Dateien im Repo hinterlässt — `@react-pdf/renderer` kann das nicht lesen, und ein Nachladen von Google zur Laufzeit wäre der GDPR-Verstoß, den `CLAUDE.md` ausdrücklich verbietet. Sobald echte Font-Dateien vorliegen (z. B. vom Vorstand oder aus Googles offiziellem Font-Repo bezogen und ins Repo gelegt), `Font.register` ergänzen |
| `src/messages/{de,en}.json` (`Mail.*`) | Freigabe der Mailtexte durch den Vorstand | — | Bewerbungsbestätigung, Erinnerungs-Bestätigungsmail und Kontakt-Weiterleitungsbetreff sind aktuell Platzhalter-Entwürfe (kurz gehalten, absichtlich leicht austauschbar über die Message-Dateien, kein Text im Code) |
| `src/content/retention.ts` | Freigabe der Aufbewahrungsfristen durch den Vorstand | — | Fristen sind gesetzt (Bewerbungen 6 Monate ab Fensterende, Kontaktanfragen 12 Monate, unbestätigte Reminder-Einträge 30 Tage), nicht erfunden — jede trägt `confirmedByBoard: false`. Eine tägliche Cron-Route (`/api/cron/cleanup`, `docs/deployment.md`) setzt sie bereits durch; nur die Zahlen selbst warten auf Bestätigung |
| `.env.local` / Vercel-Projekteinstellungen (`RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `RESEND_REPLY_TO_EMAIL`, `APPLICATION_RECIPIENT_EMAIL`) | Resend-Konfiguration in **allen** Umgebungen setzen | — | **Blocker vor dem 1. September.** Diagnose 2026-08-16: Kontaktnachrichten landen korrekt in der Datenbank, der Mailversand wird auch erreicht — er scheitert davor an fehlenden Variablen. Die einzige Zeile in `contact_messages` (2026-08-16 14:51 UTC) trägt `mail_status = 'failed'` mit `mail_error = 'RESEND_REPLY_TO_EMAIL is not set'`. In `.env.local` fehlen alle vier Variablen (die Datei ist ein reiner `vercel env pull` der Neon-Integration plus `CRON_SECRET`). Ob sie in Vercel für Preview/Production gesetzt sind, ließ sich ohne Vercel-CLI nicht prüfen — je Umgebung einzeln prüfen. Danach `npm run mail:test` (siehe `docs/deployment.md`) |
| `.github/dependabot.yml` | Wöchentlichen Lauf nach dem Launch wieder scharf stellen | — | Seit 2026-08-16 auf `schedule: monthly` und `open-pull-requests-limit: 0` gedrosselt — wöchentliche PRs erzeugten rote CI-Läufe und Mailflut kurz vor dem 1.-September-Launch. **Muss nach dem Launch zurückgesetzt werden** (`interval: "weekly"`, Limit entfernen), sonst bekommt das Projekt dauerhaft keine Sicherheitsupdates mehr mit |

## Accepted dependency advisories

`npm audit --omit=dev` reports 3 high-severity findings, all in packages that
Next.js pins internally rather than something we installed directly. The only
fix `npm audit fix --force` offers is a downgrade to `next@9.3.3`, which is not
an option.

| Package | Advisories | Pinned by | Why not forced | Resolved when |
| ------- | ---------- | --------- | --------------- | ------------- |
| `postcss` (`node_modules/next/node_modules/postcss`, 8.4.31) | [GHSA-qx2v-qp2m-jg93](https://github.com/advisories/GHSA-qx2v-qp2m-jg93), [GHSA-6g55-p6wh-862q](https://github.com/advisories/GHSA-6g55-p6wh-862q), [GHSA-r28c-9q8g-f849](https://github.com/advisories/GHSA-r28c-9q8g-f849) — vulnerable range `<=8.5.17` | `next@16.2.11` (`dependencies.postcss` is a hard-pinned version, not a range) | `next@16.2.11` is already the `latest` npm dist-tag; no stable release exists that bumps this | `npm view next@latest dependencies` shows `postcss` `>8.5.17` |
| `sharp` (`node_modules/sharp`, `^0.34.5`) | [GHSA-f88m-g3jw-g9cj](https://github.com/advisories/GHSA-f88m-g3jw-g9cj) (CVE-2026-33327, CVE-2026-33328, CVE-2026-35590, CVE-2026-35591) — vulnerable range `<0.35.0` | `next@16.2.11` (`optionalDependencies.sharp`) | Same as above — already on latest stable Next | `npm view next@latest optionalDependencies` shows `sharp` `>=0.35.0` |

Checked 2026-07-25: `next@16.2.11` is both installed and the current `latest`
dist-tag, so there is no stable upgrade to take. The `canary` channel
(`16.3.0-canary.96`) bumps `sharp` to `^0.35.3` (fixed) but only bumps `postcss`
to `8.5.10`, still inside the vulnerable `<=8.5.17` range — so even canary
would not fully clear this, and a pre-release channel is not appropriate for a
production site regardless.

Re-run `npm audit --omit=dev` after every Next.js upgrade (Dependabot now opens
those PRs automatically) to check whether these have been resolved upstream.
Do not run `npm audit fix --force` to silence them.
